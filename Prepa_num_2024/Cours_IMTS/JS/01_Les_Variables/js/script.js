//alert('hop');
/**
 *  Cours 1 : Les Variables
 * Objectifs :
 * 1) Créer variables
 * 2) Utiliser la console 
 * 3) Modifier une variable
 * 4) Les oprérateurs 
 * 5) Les constantes
 */
// Pour déclarer une variable, on utilise l'intruction let 
let a=1;
// console.log() permet d'afficher du texte ou autre dans la console 
// on affiche le contenu de la variable a dans la console
console.log(a)
// On va modifier la valeurs de a;
a=2;
console.log(a);
// Créations de la deuxième variable de b

// Une addition
let b=4;
console.log(a+b);

let resulat=a+b;
console.log(resulat);

/**
 * Exercice :
 * Créez une variable resultat
 * affectez à cette variable le resultat de la soustraction de a et b
 * (oprérateur -)
 * affichez resultat dans la console 
 * faites de même pour une mutipication (opérateur *)
 * et faites une division (opérateur /)
*/

resulat=a-b;
console.log(resulat);
resulat=a*b;
console.log(resulat);
resulat=a/b;
console.log(resulat);

// Pour créer une constant on utilise la const
const c=5;
c=4;



